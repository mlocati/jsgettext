export namespace Gettext {
    /**
     * Helper class to work with territories.
     */
    export class _____REMOVE_____PluralData {
        /**
         * The CLDR territory data.
         */
        protected static data: { [id: string]: { formula: string, cases: number[], examples: Array<string | null> } }/***** = <<CLDR_PLURALS>>*****/;
    }
}
